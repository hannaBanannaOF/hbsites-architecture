apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: core-postgres
  namespace: questmaster
spec:
  serviceName: core-postgres
  replicas: 1

  selector:
    matchLabels:
      app: core-postgres

  template:
    metadata:
      labels:
        app: core-postgres
    spec:
      containers:
        - name: postgres
          image: postgres:16
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_USER
              value: postgres
            - name: POSTGRES_PASSWORD
              value: postgres
            - name: POSTGRES_DB
              value: questmaster-core
          volumeMounts:
            - name: postgres-data
              mountPath: /var/lib/postgresql/data

  volumeClaimTemplates:
    - metadata:
        name: postgres-data
      spec:
        accessModes:
          - ReadWriteOnce
        resources:
          requests:
            storage: 10Gi